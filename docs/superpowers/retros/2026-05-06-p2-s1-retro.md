# Retro — Faza 2, Sesja 1 (Luxmed scrapper reanimacja)

**Data:** 2026-05-06
**Branch:** `feature/scrapper-reanimacja`
**HEAD:** `af04012`
**Plan:** `docs/superpowers/plans/2026-04-29-luxmed-scrapper-reanimacja.md`

## Co poszło dobrze

- **Subagent-Driven Development z dwustopniowym review** — Tasks 1-16 zbudowane bez regresji. Każdy task: failing test → impl → review → commit. Czas: jedna sesja.
- **TDD strict + `_MockAdapter`** — testy izolują HTTP bez biblioteki mockującej; łatwo iterować na shape body bez touch real API.
- **Recon-first approach (`docs/superpowers/curls/*.sh`)** — capture realnych requestów przez Playwright MCP **przed** kodowaniem dał konkretny shape do parsowania, oszczędził błądzenie.
- **JWT masking + .env w `.gitignore`** — security hygiene od początku, nie odłożone na "potem".
- **Plan z explicit tasks 1-17** — jasna granica między T16 (kod gotowy) a T17 (manual validation).

## Co poszło źle

- **6 bugów wykrytych dopiero w T17 manual validation** — wszystkie były rozsiane między spec/plan a real API. Plan był pisany na bazie recon files, ale recon też miał luki:
  1. Throttle missing → 429 rate-limit (14 GET/s).
  2. `requests.HTTPError` wybijał traceback zamiast graceful exit.
  3. Plan zakładał chunk-by-14d (`searchDatePreset=14`), reality: per-day. Refaktor → revert.
  4. Plan zakładał `termsForService.termsForDays[]` (lista), reality: `termsForDay` (dict). Parser zwracał pusto.
  5. Plan miał `searchDatePreset=14` w URL params, reality wymaga `searchDateTo + processId + searchByMedicalSpecialist + expectedTermsNumber`.
  6. Term mial `serviceVariantId` w spec, reality: `serviceId`.
- **LockTerm 400 nie rozwiązany** mimo explicit shape per recon — capture był z 2026-04-29, możliwe że Luxmed dodał coś w międzyczasie albo recon nie złapał wszystkich pól.
- **Dryf intencji w środku sesji** — moment "fizjo nie ma terminów" → przerzucenie na ortopedę → odkrycie parsera-bug. Bez tego nudgu user-a moglibyśmy nie znaleźć bugu (testy passowały, bo mockowały plan-shape, nie real-shape).
- **Brak data-shape testów na real API early** — pierwszy real call był dopiero w T15 smoke + T17 walidacja. Gdyby był jeden integration test z prawdziwym capture w T8, parser-bug wyszedłby tydzień wcześniej.

## Action points

- **Capture realnego LockTerm requestu (T#18)** — przez agent-browser/Playwright, diff z `_build_lock_term_body()` w `src/scrapper/client.py:221`. Hipoteza: brakuje pola dorzucanego przez Angular SPA albo header.
- **Refresh recon files** — `docs/superpowers/curls/one_day_terms.sh` i `lock_term.sh` mają datę 2026-04-29. Zaktualizować po T#18 jeśli capture pokaże dryf.
- **Default-add data-shape integration test** w przyszłych SDD planach — jeden test który robi prawdziwy call do API (lub używa fixture z najnowszego capture) na każdym endpoincie, żeby parser-bugi nie czekały do T17.
- **Throttle/retry-on-429 jako default** w nowych klientach skrobiących — jitter 1.2-2.0s + retry-on-429 z backoff 30s. Dodać do template/skill jeśli będę pisał następnego skrappera.

## Estimate accuracy

- Plan zakładał T1-T16 jako "implementacja zgodnie z recon", T17 = "manual validation, prawdopodobnie clean".
- Reality: T1-T16 ✓ on-time, T17 wybuchł 6 bugami z czego 5 sfixowane in-session, 1 zostaje jako Task #18.
- Wniosek: estimate dla manual validation tasków powinien zakładać **bug discovery rate ~30-50%** dla scrapperów na żywym API (Luxmed nie ma changeloga, każdy capture to migawka).

## Memory artifacts

- Snapshot: `~/.claude/projects/-home-adrian-privatespace/memory/project_state_2026-05-06-luxmed-scrapper-reanimacja.md`
- Pointer w: `MEMORY.md` → `## Current state`
